# myforge — dependencies
# Pin versions loosely; all free-tier friendly.
# User note: if litellm forces a conflicting httpx, downgrade httpx to 0.27.2.

fastapi==0.115.6
uvicorn[standard]==0.34.0
streamlit==1.41.1
litellm==1.55.10
pydantic==2.10.4
pyyaml==6.0.2
jinja2==3.1.5
httpx==0.27.2
python-dotenv==1.0.1
watchdog==6.0.0
textual==0.85.2
rich==13.9.4

# Phase 2: Smart Memory — tree-sitter AST parsing + dependency graph
tree-sitter==0.26.0
tree-sitter-python==0.23.2
networkx==3.4.2

# Phase 3-5 — optional but recommended.
chromadb==0.5.23
apscheduler==3.11.0
trafilatura==2.0.0

# Mobile HITL — Telegram bot.
python-telegram-bot==21.10

# Uncomment if you want MCP tool support (Phase 4).
# mcp==1.2.0
